        int sub(int x) {
          int y;
          return (x + y);
        }
        void main(void) {
          int x[8]; int y; int z; int w;
          while (x[w] + y * z >= z * w) {
            int p;
            x[2] = p * z + w - p;
          }
          if (x[z] + 2 > y) y = x[0] * 5; else y = z - 1000;
          x[4] = sub(w);
        }